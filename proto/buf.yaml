version: v1
deps:
  - buf.build/gogo/protobuf
#  - buf.build/tendermint/tendermint # don't use since can't specify tendermint-v0.34.19
breaking:
  use:
    - FILE
lint:
  use:
    - BASIC
    - UNARY_RPC
